# AGENTS.md

## 代码提交

- 每次执行 `git commit` 前，必须先运行 `pnpm format`，确保格式化成功并将格式化产生的改动一并提交。
- 提交前按功能模块拆分改动，每个 commit 保持合理粒度。
- commit 信息遵循项目现有风格，使用 `type(scope): 描述`；无明确模块时省略 `scope`。

## 代码推送

- 推送前需充分理解改动内容与上下文，确保提交逻辑完整且无遗漏。
- 必要时允许合并（Squash）过程性或琐碎的 commit，保持提交历史清晰紧凑，避免产生冗余记录。
